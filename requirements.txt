# Pinned to the versions that TRAINED the models, so the committed .pkl artifacts
# (models.pkl, oof.pkl, X/Y/split.pkl) unpickle cleanly on any host — e.g. Streamlit
# Community Cloud. Deploy on Python 3.13 to match the local build.
pandas==3.0.3
numpy==2.5.1
scikit-learn==1.9.0
matplotlib==3.11.1
streamlit==1.59.2
openai==2.46.0   # optional — only the Clinical Copilot narrative layer uses it
